DeAnnotate(Request)		Editor			DeAnnotate(Request)

NAME
     DeAnnotate, DeAnnotate_All - remove annotation(s)

SYNOPSIS
            DeAnnotate_All(  in      viewID      docView );
            DeAnnotate(      in      viewID      docView,
                            [in      locator     start,
                             in      vector      extent,
                             in      string      annotation]
                             [...] );

OPTIONAL ARGUMENTS
     locator start
     vector  extent
     string  annotation
          When not even one set of these arguments is included, the
          handler should remove all annotations created by the
          requestor's procID.
