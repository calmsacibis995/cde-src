Annotate(Request)		Editor			Annotate(Request)

NAME
     Annotate - annotate a view

SYNOPSIS
            Annotate(        in      viewID      2annotate,
                             in      locator     start,
                             in      vector      extent,
                             in      string      annotation
                             [...] );

REQUIRED ARGUMENTS
     string annotation
          The annotation to set.  One of
            error
            warning
            breakpoint
            breakpoint (inactive)
            current PC
            pending PC
            custom:*

OPTIONAL ARGUMENTS
     locator start
     vector  extent
     string  annotation
          Extra sets of these arguments may be included.
