laserrom.dt has been replaced in the build environment by 
laserrom.dt.src. To create laserrom.dt: at the command line
type:

	make laserrom.dt

For changes to laserrom.dt, you must edit the file 
laserrom.dt.src.

